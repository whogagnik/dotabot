from django.apps import AppConfig

class ScriptsDjangoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "scripts.host.django"
    label = "scripts_host_django"
    verbose_name = "Scripts Host Django Planner"